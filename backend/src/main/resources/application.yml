spring:
  application:
    name: fanbook-java-backend
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  datasource:
    url: ${FANBOOK_DATASOURCE_URL:jdbc:mysql://localhost:3306/fanbook?serverTimezone=UTC}
    username: ${FANBOOK_DATASOURCE_USERNAME:fanbook}
    password: ${FANBOOK_DATASOURCE_PASSWORD:fanbook}
  data:
    redis:
      host: ${FANBOOK_REDIS_HOST:localhost}
      port: ${FANBOOK_REDIS_PORT:6379}
  rabbitmq:
    host: ${FANBOOK_RABBITMQ_HOST:localhost}
    port: ${FANBOOK_RABBITMQ_PORT:5672}
    username: ${FANBOOK_RABBITMQ_USERNAME:fanbook}
    password: ${FANBOOK_RABBITMQ_PASSWORD:fanbook}
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        timezone:
          default_storage: NORMALIZE

server:
  servlet:
    session:
      timeout: ${FANBOOK_SESSION_TIMEOUT:30m}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

fanbook:
  epub:
    max-entries: ${FANBOOK_EPUB_MAX_ENTRIES:1000}
    max-expanded-size: ${FANBOOK_EPUB_MAX_EXPANDED_SIZE:100MB}
    max-entry-size: ${FANBOOK_EPUB_MAX_ENTRY_SIZE:25MB}
  bootstrap-admin:
    username: ${FANBOOK_BOOTSTRAP_ADMIN_USERNAME:}
    password: ${FANBOOK_BOOTSTRAP_ADMIN_PASSWORD:}
    email: ${FANBOOK_BOOTSTRAP_ADMIN_EMAIL:}
  storage:
    root: ${FANBOOK_STORAGE_ROOT:./runtime/storage}
  translation:
    executor:
      core-pool-size: 2
      max-pool-size: 4
    messaging:
      exchange: fanbook.jobs
      chunk-queue: translation.chunk.queue
      retry-queue: translation.chunk.retry
      dead-letter-queue: translation.chunk.dlq
      routing-key: translation.chunk
      prefetch: ${FANBOOK_TRANSLATION_MESSAGING_PREFETCH:2}
      concurrency: ${FANBOOK_TRANSLATION_MESSAGING_CONCURRENCY:4}
      listener-auto-startup: ${FANBOOK_TRANSLATION_LISTENER_AUTO_STARTUP:true}
    recovery:
      chunk-lease: 10m
      scan-delay: 60s
      stale-job-threshold: 30m
    chunk-target-characters: 6000
    max-segments-per-chunk: 40
    context-window-segments: 4
    glossary-candidate-limit: 12
    glossary: []
    max-attempts-per-chunk: 3
  ai:
    provider: ${FANBOOK_AI_PROVIDER:mock}
    base-url: ${FANBOOK_AI_BASE_URL:https://api.openai.com/v1}
    api-key: ${FANBOOK_AI_API_KEY:}
    model: ${FANBOOK_AI_MODEL:mock-translator}
    request-timeout: ${FANBOOK_AI_REQUEST_TIMEOUT:90s}
    max-concurrency: ${FANBOOK_AI_MAX_CONCURRENCY:2}
    endpoint: ${FANBOOK_AI_ENDPOINT:}
    min-request-interval: ${FANBOOK_AI_MIN_REQUEST_INTERVAL:0s}
    thinking-mode: ${FANBOOK_AI_THINKING_MODE:}
    json-mode: ${FANBOOK_AI_JSON_MODE:true}
